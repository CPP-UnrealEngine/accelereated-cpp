#include <iostream>
int main(){
    /* this is a comment that extends over severeal lines
    because it uses /* and */ as startng and ending delimiters */
    std::cout << "Does this work?" << std::endl;
    return 0;
}